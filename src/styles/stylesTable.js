import { makeStyles } from "@material-ui/core/styles";

export const importStyle = makeStyles({
  root: {
    width: "100%",
  },
  container: {
    maxHeight: 440,
  },
});

export default { importStyle };
